# ── infra stack · env template ──
# Docker Compose tự đọc ./.env khi chạy lệnh TỪ thư mục infra/docker,
# hoặc dùng: docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml up -d
DB_USER=abs
DB_PASS=abs123
