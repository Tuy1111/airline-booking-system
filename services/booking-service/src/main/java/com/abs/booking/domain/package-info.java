/**
 * Domain layer — entity, value object, domain service, repository interface.
 * Chứa business rule thuần, KHÔNG phụ thuộc Spring/JPA.
 * VD: Booking, Ticket, SeatHold, BookingStatus enum, BookingRepository (interface).
 */
package com.abs.booking.domain;
