/**
 * Infrastructure layer — implementation cho domain repository,
 * external client (REST/Kafka/Redis), persistence (JPA entity + mapper).
 * VD: BookingJpaRepository, RedisSeatHoldStore, FlightServiceClient, KafkaEventPublisher.
 */
package com.abs.booking.infrastructure;
