/**
 * Domain layer — entity, value object, domain service, repository interface.
 * VD: Notification, NotificationTemplate, NotificationChannel enum (EMAIL/SMS).
 */
package com.abs.notification.domain;
