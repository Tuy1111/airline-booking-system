package com.abs.template;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Smoke test — context load được với H2 (chứng minh wiring các adapter ↔ port OK). */
@SpringBootTest
class ServiceTemplateApplicationTests {

    @Test
    void contextLoads() {
    }
}
